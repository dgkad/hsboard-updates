# 教室端 v1.0.8 功能完整性报告

- 验证日期：2026-09-13
- 验证对象：`dist/board_v1.0.8.zip`（GitHub Release v1.0.8 发布产物）
- 产物校验：本地 zip SHA256 `C2484A6F…FEBDB` 与 GitHub Release 摘要、线上 `update.json` 三者一致
- 验证方式：项目记忆 + 更新日志条目核对 → 鼠标控制（Computer Use / SendInput）实测 + 程序日志断言

## 一、项目结构与记忆要点（背景）

| 目录 | 功能 |
|---|---|
| `pc/` | 教室端主程序 `board.py`（Tk 桌面留言板，当前 VERSION=1.0.8）、subscriber 调试器、打包产物 |
| `parent/` | 家长端单页应用（index.html + 本地化 mqtt.min.js） |
| `config/` | broker.json（v2 混淆）、classes.json 三班配置、students/c8·c10·c13.json 名单 |
| `tools/` | 打包/发布/验证脚本（publish_board、build_parent、verify_ui_layout、wind_verify* 等） |
| `dist/` | 交付产物：pc-bundle、parent-web、board_v1.0.x.zip 序列 |
| `hsboard-updates/` | 自动更新仓库镜像（update.json + Release 附件） |
| `docs/`、`shared/`、`logs/`、`build/`、`.github/` | 使用指南、跨端 schema、运行日志、构建中间产物、发布工作流 |

项目记忆硬约束均已体现在 1.0.8 产物中：EMQX Serverless、v2 混淆 broker.json、retained ≤2000、client_id ≤23 字符、私有更新仓库 + gh-proxy。

## 二、更新日志条目与验证结论（GitHub Release v1.0.8）

> 原文：①修复拖拽缩放后扩出区域露出深色矩形（磨砂背景未随窗口拉伸）——拖拽中底图实时拉伸、全局尺寸同步更新，松手后重新清晰采样；②修复拖大窗口的高度会被自适应回收——现在用户拖大的高度优先保留；③拉宽/拉高实测无黑边，内容按新尺寸重排。

| # | 条目 | 结论 | 关键证据 |
|---|---|---|---|
| 1 | 拖拽缩放无深色矩形 | **通过** | 拖拽中 5 帧中间渲染日志（W=722→744→752、H=1331→1351→1371→1392→1408）证明 `_bg_stretch_photo` 实时拉伸生效；松手后 `[玻璃] 背景已重新采样`；放大后截图整窗磨砂覆盖，右侧/底部扩出区无深色矩形 |
| 2 | 拖大高度优先保留 | **通过** | H=1408 稳定保持 >2.5 分钟（08:54:16 起）。此时 avail 自适应上限仅 ≈1321（1440−32−87）、内容自适应亦不会收敛到 1408 —— 唯有 `_h_min`（board.py:1267-1268、1646）能使 H 停在 1408，v1.0.7 的"回收"行为未复现 |
| 3 | 拉宽/拉高无黑边、内容重排 | **通过** | 宽 700→1206（+72%）、高 1311→1408（触达屏幕上限）后截图：标题栏/时钟/卡片/滚动条/底部提示全部按新尺寸重排，四角圆角完整，无黑边、无残留兜底色 |

## 三、实测过程

**环境**：解包 board_v1.0.8.zip 至临时目录运行 `board.exe`；`HSBOARD_DEMO_STATE=1`（16 张演示卡、不连 Broker、跳过自更新）、`HSBOARD_DEBUG_LAYOUT=1`（每秒输出 `[布局] … W= H=`）；班级 高一(8)班(c8)，窗口停靠"左上"。

**步骤与结果**：

1. **基线**：700×1311 @ (42,42)，磨砂玻璃 + 16 卡渲染正常（见 `verify-v1.0.8/01-initial-700x1311.jpg`）。
2. **SE 角外拖（第 1 次）**：700×1311 → 752×1408（受屏幕右/下边界限位）。日志序列：
   - 拖拽中：`[布局] … W=722 H=1331` → `W=744 H=1351` → `W=752 H=1371` → `W=752 H=1392` → `W=752 H=1408`（拖拽中 render 持续以新尺寸重绘 = 底图实时拉伸 + 内容同步重排）
   - 松手：`[玻璃] 背景已重新采样`（清晰重采样），此后 H=1408 长期稳定（条目 2 证据）
3. **SE 角外拖（第 2 次，窗口复位左上后）**：700×1311 → 1206×1408。截图确认整窗磨砂、布局按新宽重排、无黑边（`03-after-widen-1206x1408.jpg`）。
4. **缩小方向回归**：E 边内拖时 W 精确跟随指针收缩至下限 Z(300)=525 —— 缩小路径正常（"拖大优先保留、拖小仍回收"的对称行为，代码 board.py:1267-1271 同一路径）。
5. **发布链路核对**：`raw.githubusercontent.com/.../update.json` 已指向 1.0.8，sha256 与 Release 附件一致，changelog 同步 —— 教室端开机自更新可正确收到 1.0.8。

**截图证据**（`verify-v1.0.8/`）：

![基线 700x1311](verify-v1.0.8/01-initial-700x1311.jpg)
![第一次放大后 752x1408 无深色矩形](verify-v1.0.8/02-after-SE-drag-752x1408.jpg)
![大幅放大后 1206x1408 内容重排无黑边](verify-v1.0.8/03-after-widen-1206x1408.jpg)
![未读/已读分区与展开回执](verify-v1.0.8/04-read-unread-zones.jpg)
![家长端已读回执回转](verify-v1.0.8/05-parent-read-receipt.jpg)

## 四、局限性说明

1. 拖拽进行中的屏幕截图无法捕获（拖拽为原子操作），以日志中间帧 + 松手后截图替代证明。
2. S 边单独缩高未能命中热区（自动化拖拽坐标映射偏差），高度缩小回收以同代码路径的宽度收缩结果佐证。
3. 首次拖拽后出现过一次 `[桌面] 桌面层合成异常，进入浮动模式`（08:34:50），为既有机制（自动置顶保活并尝试归位），与本次三条目无关，建议持续观察。
4. 希沃真机触摸（ButtonRelease 判定、B1-Motion 滚动）属项目记忆中遗留的人工复核项，本报告未覆盖真机触控。

## 五、结论

**v1.0.8 更新日志所列 3 项条目全部验证通过**，发布产物哈希与更新链路完整，具备存量教室端推送条件。

---

# 附录：v1.0.1→v1.0.8 核心功能回归测试（消息链路专项）

- 测试日期：2026-09-13（同日续测）
- 隔离方案：**全程使用虚构班级 `ct9`**（topic 段 `hsdemo/ct9/#`，真实班级 c8/c10/c13 零接触）；教室端以 `HSBOARD_CLIENT_ID=dev-test-ct9`（独立持久会话）运行发布产物 exe；家长端用 `dist/parent-web` 隔离副本（classes.json 仅含 ct9）本地起站测试
- 断言手段：`board_run.log` 结构化日志（[班级]/[更新]/[连接]/[恢复]/[留言]/[回执]/[喇叭]/[布局]）+ paho 脚本收发探针 + 浏览器 UI 检查 + 截图
- 测试后清理：教室端进程已停止；`hsdemo/ct9/#` 全部 5 个 retained（ack×3、hist×2）已发布空载荷清除并验证归零；临时目录已删除

## 一、消息处理逻辑（教室端核心链路）

| # | 功能（版本来源） | 结论 | 关键证据 |
|---|---|---|---|
| 1 | 开机 hist 快照找回（v1.0.4 关机期间留言不丢） | **通过** | 预置 `hist/901` retained（3 条）+ `ack/901`（1 条累计已读）→ 启动后日志 `[恢复] 从服务端历史恢复 901 2 条留言（未读红点已亮出）`：2 条未读恢复亮红点，**已读的 1 条被 ack seen 正确跳过（防死灰复燃）** |
| 2 | 在线收信 + 未读红点（v1.0.x 基础） | **通过** | `send_test.py` 发 902 → 卡片置顶出现（红点 1，未读最新最顶） |
| 3 | 点击展开即已读 + **回执立即发布**（v1.0.2 点击后 10 秒延迟移区） | **通过** | 鼠标点击卡片 → 教室端立刻发布累计回执（日志 `[回执] … 本次1条`），家长端脚本收到 `retained=True` 回执；**点击后约 10 秒卡片移入已读区**（日志 read_zone 0→1） |
| 4 | 累计回执（msgIds ≤200，v1.0.2） | **通过** | `ack/902` retained 内容 `{"msgIds":["t-62807d6e","t-c273604b"],…}` —— 两次已读合并为一条累计回执；乙出现新未读时卡片自动回到未读区 |
| 5 | QoS1 去重（v1.0.x） | **通过** | 重发同 msgId → 无新卡片、无新 [留言] 日志 |
| 6 | 离线排队（关机不丢，v1.0.x 持久会话） | **通过** | 杀进程 → 发 2 条 → 重启同 client_id → 连上即补收（09:16:16 两条 [留言]），且与 hist retained 二次恢复去重无重复 |
| 7 | 老师通道 `t-姓名`（v1.0.2 老师普通/紧急） | **通过** | 发 `t-测试老师`（role=teacher）→ 卡片显示"王老师"而非学号 |
| 8 | 紧急通知 horn → ClassIsland 转发（v1.0.2） | **通过（有观察项）** | `[喇叭] 收到 李老师 的紧急通知` → `[喇叭] ClassIsland(ZMQ) 紧急通知已展示`；horn 卡片置顶未读且不自动展开（内容不上墙）。⚠观察项：本机并无 ClassIsland 进程但 5555 端口有应答方返回 success，建议排查端口占用 |
| 9 | 畸形消息压测（v1.0.4 安全加固） | **通过（7/7）** | 超长文本截断 600 字；非法 UTF-8 忽略；非对象 JSON 忽略；text 数字/ts 字符串容错；控制字符换行转空格；重复 msgId 去重；名单外学号 999 正常建卡"学号999"。全程进程存活 |

## 二、教室端 UI / 配置功能

| # | 功能（版本来源） | 结论 | 关键证据 |
|---|---|---|---|
| 1 | 未读/已读分区 + 双区排序（v1.0.2 八项之一） | **通过** | 截图 `04-read-unread-zones.jpg`：未读区在上（红点计数）、已读区在下（展开显示两条留言+时间戳）；回归脚本断言 first_unread=demo00 / first_read=demo15 |
| 2 | 位置提示 + 滚动条（v1.0.2） | **通过** | 回归脚本：above_n/below_unread 仅统计未读、thumb 合法且滚底位于轨道下部 |
| 3 | 缩放档位 25%/50%（v1.0.2） | **通过** | --scale 0.5 → W=350（基准减半）、0.25 → W=175（四分之一），布局仍可滚动 |
| 4 | 三档密度 cozy/compact/grid（三班发行版） | **通过** | 三档 density 字段正确、分区排序不变、布局各自成立 |
| 5 | 自动更新检查（v1.0.1） | **通过** | 启动日志 `[更新] 已是最新版 1.0.8（本地 1.0.8）`，无误替换 |
| 6 | 班级机制 + topic 隔离（三班发行版） | **通过** | `[班级] 当前班级: 测试班（ct9）`，订阅 `hsdemo/ct9/msg/#`、`hist/#`、`ack/#` 全部带班级段 |
| 7 | 单实例锁（按班级） | **通过** | 二次启动打印 `[启动] 留言板已在运行，本实例退出（请勿重复开启）`，窗口数保持 1 |
| 8 | v2 混淆 broker.json（v1.0.4） | **通过** | 教室端/工具链/家长端三方均正常解密连接同一 EMQX Serverless |

> 官方回归套件 `tools/verify_ui_layout.py`（演示模式零网络）**31 项断言全部 PASS**。

## 三、家长端网页（PWA）

| # | 功能 | 结论 | 关键证据 |
|---|---|---|---|
| 1 | 登录页：角色选择 + 班级下拉 | **通过** | "我是家长/我是老师"、班级下拉仅"测试班"（隔离副本生效） |
| 2 | 学生搜索过滤（姓名/拼音/首字母） | **通过** | 输入"丙"→ 实时过滤"测试同学 丙"；**键盘 ↑+Enter 选中**（"已选：测试同学丙"） |
| 3 | WSS 连接 EMQX Serverless | **通过** | 进入留言板后 `已连接`（wss://…:8084/mqtt；诊断连接与订阅均正常） |
| 4 | 发送留言 → 教室端实时收到 | **通过** | 页面显示"已发送 09:32" → 教室端 09:32:22 `[留言] 测试同学丙(903) 内容=家长端浏览器链路测试留言` |
| 5 | 已读回执回转（教室端点击后） | **通过** | 教室端鼠标点击展开 → 家长端消息状态实时翻转为 **"已读 13日 09:33"**（截图 `05-parent-read-receipt.jpg`） |
| 6 | hist 快照上传（换设备找回） | **通过** | 发送后 broker 出现 `hist/903` retained（173B） |

## 四、测试期间发现与建议

1. **【低危·观察】5555 端口异常应答**：ClassIsland 未运行，但 horn 的 ZeroMQ 通道收到 `{"success":true}`。若教室机存在其它占用 5555 的软件，可能误报"紧急通知已展示"。建议后续在 IslandMQ 握手中加入约定字段校验。
2. **【提示·已知设计】同学生多设备互踢**：家长端 clientId 固定为 `p-<学号>-<随机4>`，同一孩子的两个浏览器同时在线会互相踢（双方反复"重连中"）。家庭双设备场景建议在指南中说明，或后续引入设备随机后缀的会话协商。
3. **【记录】自动化测试注意事项**：家长端"连接中…"为静态占位文案，仅在点击"进入留言板"后才真正发起连接（未选孩子时被 alert 拦截返回）；此前一次 purge 探测为 0 系订阅→retained 投递窗口过短的竞态，retained 实际未丢，加长等待后复测 5 个主题俱在、随后成功清除。

## 五、附录小结

v1.0.1（自动更新/配置打包）→ v1.0.2~1.0.4（分区/回执/缩放/触控/安全加固）→ v1.0.5（gh-proxy 更新链路）→ v1.0.6~1.0.7（拖拽缩放/窗口行为）→ v1.0.8（缩放黑边/高度保留）的**核心功能全部实测可用**；真实班级数据全程零接触，测试残留已清零。
