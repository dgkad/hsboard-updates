# -*- coding: utf-8 -*-
"""生成《家校留言板 · 使用指南》单文件 HTML（截图 base64 内嵌，可直接分发/打印）。

结构：① 必看使用指南 ② 更新日志 ③ 技术文档（普通人能理解的架构与运行逻辑）
截图容错：文件存在才插入图片框，不存在则跳过（不报错）。
"""
import base64
import os

ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
OUT = os.path.join(ROOT, "docs", "家长老师使用指南.html")
TMP_SHOT = r"C:\Users\admin\AppData\Local\Temp\trae\screenshots\docs"


def b64(path):
    with open(path, "rb") as f:
        return base64.b64encode(f.read()).decode()


def img_tag(path, css="max-width:100%", caption=""):
    """文件存在才生成图片框（容错），不存在返回空字符串"""
    if not os.path.exists(path):
        return ""
    tag = '<div class="imgbox"><img src="data:image/png;base64,%s" style="%s"></div>' % (b64(path), css)
    if caption:
        tag += '<div class="cap">%s</div>' % caption
    return tag


# 截图容错：存在才加载
BOARD_IMG = img_tag(os.path.join(ROOT, "logs", "board_shot10.png"), "max-width:82%",
                    "教室电脑桌面上的留言板：红点=未读，点击卡片才显示内容")

html = """<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>家校留言板 · 使用指南</title>
<style>
  body { font-family: "Microsoft YaHei", sans-serif; color: #2c3e50; margin: 0;
         background: #f4f6f8; line-height: 1.75; }
  .page { max-width: 760px; margin: 0 auto; background: #fff; padding: 36px 44px 60px; }
  h1 { font-size: 26px; margin: 0 0 4px; }
  .sub { color: #8a97a5; font-size: 14px; margin-bottom: 28px; }
  h2 { font-size: 19px; margin: 34px 0 10px; padding-left: 10px;
       border-left: 4px solid #4a90d9; }
  h3 { font-size: 16px; margin: 20px 0 6px; color: #3a5a8a; }
  p, li { font-size: 15px; }
  .imgbox { text-align: center; margin: 14px 0 6px; }
  .imgbox img { max-width: 100%; border: 1px solid #dce3ea; border-radius: 8px; }
  .cap { font-size: 13px; color: #8a97a5; text-align: center; margin-bottom: 14px; }
  .tip { background: #eef5ff; border-radius: 8px; padding: 12px 16px; font-size: 14px; }
  .log { background: #f8f9fa; border-radius: 8px; padding: 14px 18px; font-size: 14px;
         border-left: 3px solid #6c7a89; margin: 8px 0; }
  .log .ver { font-weight: bold; color: #4a90d9; }
  .st-sent { color: #d48806; font-weight: bold; }
  .st-read { color: #1a9e55; font-weight: bold; }
  .horn { color: #d4380d; font-weight: bold; }
  .arch { background: #f0f4f8; border-radius: 10px; padding: 16px 20px; margin: 14px 0;
          font-size: 14px; line-height: 2; }
  .arch b { color: #2c3e50; }
  .faq { margin: 10px 0; }
  .faq q { font-weight: bold; color: #3a5a8a; }
  .sep { border: none; border-top: 2px dashed #dce3ea; margin: 36px 0; }
  @media print { body { background: #fff; } .page { padding: 10mm; } }
</style>
</head>
<body>
<div class="page">
  <h1>家校留言板 · 使用指南</h1>
  <div class="sub">电脑委员 整理 · 有问题随时来教室找我</div>

  <!-- ============ 第一部分：必看使用指南 ============ -->

  <h2>一、写给老师：我做这个的目的和效果</h2>
  <p>各位老师好，我是咱们班的电脑委员。做这个留言板，是想解决几个日常小麻烦：
  家长有急事找不到老师、打电话怕打扰上课；班级群消息多，重要的事容易被刷走；
  让同学带话又容易漏。</p>
  <p>现在教室电脑桌面上常驻一块"班级留言板"，效果是这样的：</p>
  <ul>
    <li>家长在手机上留言，教室屏幕上出现<b>红点提示</b>；</li>
    <li><b>留言内容默认隐藏</b>，学生点击卡片才展开——不会把任何人的消息直接晒在公共屏幕上；</li>
    <li>学生点开的那一刻，家长手机上立刻变成<b>"已读"</b>，不用再回复"收到了"；</li>
    <li>有需要全班马上知道的事，用<span class="horn">紧急通知</span>：教室电脑会<b>全屏醒目提示</b>，留言板里也会留档。</li>
  </ul>
  __BOARD_IMG__

  <h2>二、家长怎么用（4 步）</h2>
  <p>① 手机浏览器打开：<b>https://liuyan.dengjy.xyz</b><br>
     ② 在页面顶部<b>选择班级</b>（选一次，下次自动记住）<br>
     ③ 搜索并选择自己孩子的姓名 → 点"进入留言板"<br>
     ④ 输入留言 → 点"发送"</p>
  <p>留言状态就两种：<span class="st-sent">已发送</span> = 已送到教室；
     <span class="st-read">已读</span> = 教室里已经点开看过了。看到"已读"就放心吧。</p>
  <p>看到"已读"后面跟着的<b>日期和时间</b>，就是孩子点开留言的确切时刻。</p>

  <h2>三、老师怎么用</h2>
  <ol>
    <li>打开同一个网址 → 点"我是老师" → <b>先选班级</b> → 输入自己的身份（比如"数学老师"）→ 进入；</li>
    <li><b>普通模式</b>：像家长一样给全班留言，教室留言板会出现卡片；</li>
    <li><b>紧急通知</b>：教室电脑立刻全屏醒目提示，适合"马上改集合地点"这类等不了的事；</li>
    <li>普通留言和紧急通知在<b>同一条历史记录</b>里，随时能回看自己发过什么、有没有被看到。</li>
  </ol>

  <h2>四、小贴士</h2>
  <div class="tip">
    · 页面偶尔显示"已断开"不用管，它会自动重连；刷新一下也行，<b>历史不会丢</b>。<br>
    · 留言在教室屏幕上默认隐藏、点击才展开，隐私放心。<br>
    · <b>最新消息显示在列表最上面</b>，不用往下滑着找。<br>
    · 每人最多保留最近 200 条记录，超出自动删最旧的。<br>
    · 换手机或清过浏览器缓存也没关系，历史记录在服务器上也存着，重新登录就回来。<br>
    · 打不开、看不懂的，课间来教室找我
  </div>

  <h2>五、设置菜单（右键面板）逐项说明</h2>
  <p>在教室留言板卡片上<b>右键</b>会弹出设置菜单，每一项的作用如下：</p>
  <ul>
    <li><b>班级</b>：决定这台教室电脑订阅哪个班（8 / 10 / 13）。选一次会自动记住并重启程序生效，之后每次开机都用这个班。三台教室电脑各选各的，互不干扰。</li>
    <li><b>位置</b>：换面板在屏幕上的停靠位置（右上角 / 左上角 / 右下角…），适合被桌面美化软件挡住的情况。</li>
    <li><b>每条留言最多显示 N 行</b>：卡片展开后正文最多几行。数字越小卡片越窄、一屏能排的人越多；设为 0 表示显示完整内容。</li>
    <li><b>缩放比例</b>：25% / 50% / 75% / 100%，按教室投影仪分辨率调大小，文字太小看不清就调大。</li>
    <li><b>卡片密度</b>：<b>标准</b>（一屏约 11 人）/ <b>紧凑</b>（约 20 人）/ <b>双列网格</b>（约 40 人）。人多的班选密度高的。</li>
    <li><b>开机自启</b>：勾选后教室电脑开机自动运行留言板，不用每次手动点。</li>
    <li><b>玻璃磨砂背景 / 重新采样</b>：让面板背景跟随桌面壁纸做磨砂效果，"重新采样"是重新取一次壁纸颜色。</li>
  </ul>

  <!-- ============ 第二部分：更新日志 ============ -->

  <hr class="sep">
  <h2>六、更新日志</h2>
  <p>这个留言板会持续改进。下面记录每次更新做了什么，方便老师和家长了解新功能。</p>

  <div class="log">
    <span class="ver">2026-09-13 · 体验优化 &amp; Bug 修复</span><br>
    · <b>缩放后位置不再乱飞</b>：改版位置记忆，切缩放比例时面板固定贴角，不跑到屏幕中间<br>
    · <b>已读区改为最早消息显示在最上方</b>：点击已读的卡片，先发的排在最前，后发的依次向下排列<br>
    · <b>"上方/下方还有N位"只计未读</b>：已读的同学不计入提示数量，只提示仍有未读的待处理<br>
    · <b>展开卡片文字不再重叠</b>：留言内容与时间的水平间距加宽，所有屏幕比例下排版清晰<br>
    · <b>版本号从 1.0.1→1.0.2</b>：之前 1.0.0→1.0.1 修了自更新互斥锁 + 打包结构
  </div>

  <div class="log">
    <span class="ver">2026-09-13 · 自动更新 &amp; 健壮性</span><br>
    · <b>教室端自动更新</b>：每次开机先检查新版本，有更新自动下载替换重启，电脑委员不用再拿 U 盘拷文件<br>
    · <b>紧急通知降级有反馈</b>：ClassIsland 没开时紧急通知降为卡片，老师端能收到"已降级"提示，不再静默<br>
    · <b>生产排查日志</b>：关键事件（连接、断连、重置、降级、更新）都写入运行日志，便于定位一周内的问题<br>
    · <b>普通模式文案修正</b>：老师"普通模式"准确说明"以该老师身份发送普通通知"，发到老师通道、学生不显示<br>
    · <b>时钟漂移修复</b>：7 天历史边界改为以消息时间戳为基准，两端时钟不一致也不会误丢消息<br>
    · <b>家长端消息 ID 防撞</b>：改用 crypto.randomUUID，极端并发也不会撞车<br>
    · <b>切班级更稳</b>：换班级时先断开旧连接再重连，订阅和历史前缀保持一致，不会残留旧班消息
  </div>

  <div class="log">
    <span class="ver">2026-09-12 · 多班级版</span><br>
    · <b>一个网页支持多个班级</b>：登录页新增"班级"下拉，家长和老师先选班级再操作，
      选一次自动记住，下次不用重选<br>
    · <b>三班互不串扰</b>：8 班、10 班、13 班各有独立的消息通道和历史记录<br>
    · <b>教室端可切换班级</b>：右键设置 → 班级 → 选择本班，自动重启生效，
      之后开机免配置<br>
    · <b>卡片密度可调</b>：标准（一屏约 11 人）/ 紧凑（约 20 人）/ 双列网格（约 40 人），
      适应不同屏幕大小<br>
    · <b>安全加固</b>：服务器连接密码已加密，软件打包后不可直接读取<br>
    · <b>时间显示完善</b>：消息时间改为"X日 HH:MM"格式，隔天消息不再混淆<br>
    · <b>已读时间</b>：家长端显示孩子点开留言的确切时间<br>
    · <b>触控支持</b>：希沃白板触摸滑动翻看留言，与鼠标滚轮体验一致
  </div>

  <div class="log">
    <span class="ver">2026-09-05 · 正式上线版</span><br>
    · <b>未读/已读分区</b>：未读卡片排在上面（带红点），已读的自动沉到下面<br>
    · <b>10 秒延迟移区</b>：点击卡片后等 10 秒才移入已读区，不影响正在阅读<br>
    · <b>位置提示</b>：列表上下方显示"还有 N 位同学"，右侧滚动条显示当前位置<br>
    · <b>软件缩放</b>：设置里可选 25%/50%/75%/100% 缩放比例<br>
    · <b>每日自动清空</b>：教室端每天零点清空当日显示（服务器保留未读消息不丢）<br>
    · <b>关机不丢消息</b>：教室关机期间的留言，开机后自动补收<br>
    · <b>PC 端打包</b>：教室电脑无需安装 Python，双击 exe 即可运行
  </div>

  <!-- ============ 第三部分：技术文档 ============ -->

  <hr class="sep">
  <h2>七、它是怎么工作的（技术文档）</h2>
  <p>这一节用大白话讲清楚留言板背后的运作原理。不需要任何技术背景也能看懂，
  主要是帮大家理解"为什么有时消息会延迟""为什么关机后消息不丢"这些日常问题。</p>

  <h3>1. 整体架构：三段式</h3>
  <div class="arch">
    <b>家长端</b>（手机网页）→ <b>MQTT 服务器</b>（云端）→ <b>教室端</b>（桌面挂件）<br>
    <br>
    家长在手机上打开网页 → 网页连到云端的 MQTT 服务器 → 服务器把消息转发给教室电脑上的留言板程序。<br>
    反过来也一样：教室端点开留言后，"已读"回执原路发回 → 服务器 → 家长手机上显示"已读"。
  </div>
  <p>MQTT 是一种专门为消息传递设计的协议，类似于"快递中转站"：
  你把留言交给中转站，中转站负责送到教室。即使教室暂时关着，中转站也会帮你暂存。</p>

  <h3>2. 消息怎么走</h3>
  <p>以家长发一条留言为例，整个过程是这样的：</p>
  <ol>
    <li>家长在手机上输入留言，点"发送"；</li>
    <li>网页把留言发给 MQTT 服务器（云端），同时手机上显示<span class="st-sent">已发送</span>；</li>
    <li>服务器把留言转发给教室电脑，留言板出现<b>红点</b>提示有新消息；</li>
    <li>学生点击卡片展开留言；</li>
    <li>教室端立刻发一条"已读"回执给服务器；</li>
    <li>服务器把"已读"转发给家长手机，显示变成绿色的<span class="st-read">已读</span> + 时间。</li>
  </ol>
  <p>整个链路通常在 1~2 秒内完成。如果教室电脑关着，消息会在服务器排队，开机后自动送达。</p>

  <h3>3. 消息存在哪里（不会丢的秘密）</h3>
  <p>留言板有<b>三重存储</b>，确保换设备、关机、断网都不会丢消息：</p>
  <ul>
    <li><b>家长手机本地</b>（浏览器 localStorage）：家长发的每条留言都存在手机里，永不清空。
        换手机或清缓存也不怕——下面的服务器备份兜底。</li>
    <li><b>MQTT 服务器</b>（retained 消息）：每次发消息时，网页会把一份"历史快照"上传到服务器。
        服务器永远保留最后一份快照，换设备登录时自动恢复。</li>
    <li><b>教室电脑本地</b>：教室端只保留当天需要显示的，每天零点自动清空（但服务器上的不删）。</li>
  </ul>
  <p>所以即使教室电脑被重装系统，只要家长端还在、服务器还在，历史记录就不会丢。</p>

  <h3>4. 每日清空：为什么昨天的消息今天不见了</h3>
  <p>教室端的留言板<b>每天零点自动清空</b>当天显示，这主要有两个原因：</p>
  <ul>
    <li>公共屏幕上不宜长期展示旧留言（隐私）；</li>
    <li>每天看当天的新消息更清晰，不会被旧消息淹没。</li>
  </ul>
  <p>但<b>家长端永不清空</b>——你在手机上随时能翻看以前发过的所有留言和已读记录。
  如果教室关机时家长发了消息，这条消息会留在服务器的快照里，教室开机后自动恢复出来。</p>
  <div class="tip">
    注意：服务器免费版的会话保持只有 2 小时，教室关机超过 2 小时后，
    排队消息会被丢弃。但<b>历史快照不受影响</b>——它永远保留最后一份，
    所以即使排队消息丢了，家长下次发消息时快照会补上之前未读的那条。
  </div>

  <h3>5. 多班级隔离：三个班怎么互不串扰</h3>
  <p>三个班共用同一个服务器和同一个网页，靠的是<b>消息通道加班级编号</b>来隔离。</p>
  <p>打个比方：8 班的消息走"8 班通道"，10 班的走"10 班通道"，13 班的走"13 班通道"。
  每个班只收自己通道里的消息，不会被别的班看到。具体来说：</p>
  <ul>
    <li>家长登录时选了哪个班，消息就发到哪个班的通道；</li>
    <li>教室端在设置里选了哪个班，就只订阅哪个班通道的消息；</li>
    <li>每个班的历史记录也在各自的通道里分开存。</li>
  </ul>
  <p>所以即使三台教室电脑同时开着，也不会互相干扰。</p>

  <h3>6. 隐私与安全</h3>
  <ul>
    <li><b>留言内容默认隐藏</b>：教室屏幕上只显示姓名和红点，内容必须点击才展开——
        不会把任何家长的话直接晒在公共屏幕上。</li>
    <li><b>服务器密码加密</b>：连接服务器的用户名和密码在软件里是加密存储的，
        打包后用记事本打开配置文件看到的是乱码，不能直接读取。</li>
    <li><b>不收集个人信息</b>：留言板只传输留言文字本身，不采集位置、设备信息等。</li>
  </ul>

  <h3>7. 教室端桌面挂件</h3>
  <p>教室电脑上的留言板不是普通窗口，而是<b>桌面层挂件</b>——它"贴"在桌面上，
  像壁纸的一部分一样常驻显示：</p>
  <ul>
    <li>其他窗口可以正常盖住它（不会挡住老师用电脑讲课）；</li>
    <li>按 Win+D 显示桌面时它会自动出现（1 秒内回来）；</li>
    <li>右键可以调出设置菜单：选班级、换位置、调密度、调缩放、开机自启等。</li>
  </ul>
  <p>如果教室装了 ClassIsland 软件，老师的紧急通知还能触发 ClassIsland 全屏醒目提示。</p>

  <h3>8. 常见疑问</h3>
  <div class="faq">
    <p><q>为什么有时候消息发出去很久才显示"已读"？</q><br>
    可能是教室电脑关着或者网络不稳定。消息会在服务器排队，教室开机联网后自动送达。
    如果超过 2 小时教室还没开，排队消息会被丢弃，但下次家长发消息时历史快照会补上未读的那条。</p>
  </div>
  <div class="faq">
    <p><q>教室端看不到某个班的留言怎么办？</q><br>
    八成是设置里班级选错了。右键面板 → 班级 → 选对本班，程序会自动重启一次，之后就正常了。</p>
  </div>
  <div class="faq">
    <p><q>家长端选了别的班能看到别的班的留言吗？</q><br>
    不能。每个班的消息走独立通道，选了哪个班就只看哪个班的消息和历史。</p>
  </div>
  <div class="faq">
    <p><q>换手机或清理了浏览器缓存，历史会丢吗？</q><br>
    不会。手机本地存储清了没关系，服务器上还存着历史快照。重新登录同一通道就自动恢复。</p>
  </div>

</div>
</body>
</html>
"""

# 截图占位符替换（容错：不存在则为空）
html = html.replace("__BOARD_IMG__", BOARD_IMG)

os.makedirs(os.path.dirname(OUT), exist_ok=True)
with open(OUT, "w", encoding="utf-8") as f:
    f.write(html)
print("已生成:", OUT, f"({os.path.getsize(OUT)//1024} KB)")
